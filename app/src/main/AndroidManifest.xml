<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashiswin.kodyac">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <meta-data
        android:name="com.google.android.gms.vision.DEPENDENCIES"
        android:value="ocr" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VerificationMethodsActivity" />
        <activity android:name=".SMSVerificationNumber" />
        <activity android:name=".SMSVerificationOTP" />
        <activity android:name=".NRICVerificationCard" />
        <activity android:name=".NRICVerificationSelfie" />
        <activity
            android:name=".NRICBarcodeActivity"
            android:screenOrientation="sensorPortrait" />
        <activity android:name=".OpenCVActivity"></activity>
    </application>

</manifest>